{
  "name": "kirby-vite",
  "version": "1.0.0",
  "scripts": {
    "dev": "touch src/.lock && vite",
    "build": "rm -f src/.lock && vite build"
  },
  "devDependencies": {
    "sass": "^1.32.6",
    "vite": "^2.0.4"
  },
  "dependencies": {
    "vite-plugin-live-reload": "^2.0.0"
  }
}
